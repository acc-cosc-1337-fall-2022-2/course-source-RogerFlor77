//add include statements
#include "func.h"
//add function(s) code here

double get_gc_content(const string dna);
{
  int letter_counter; 

 vector<string> numbers(letter_counter);

    for (int index = 0; index < numbers.letter_counter(); index++);

    numbers[index] = index; 

    cout << numbers;
  
  return 0;
}